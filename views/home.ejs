<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= locals.title %>
    </title>
    <script src="https://kit.fontawesome.com/514d9df4a5.js" crossorigin="anonymous"></script>
    <!-- our css file should below all the other style so that we can overide it -->
    <link rel="stylesheet" href="/css/home.css">
    
</head>
<body>
    
    <h1 id="page-heading">
        <i class="fas fa-address-book"></i>
        My Contact List</h1>
    <div class="contact-list-container">   
            <ul>
                <% for(let i of contact_list){%>
                     
                        <li class= "contact">
                            <div class="details">
                                <p><%= i.name %></p>          
                                <p><%= i.phone %></p> 
                            </div> 
                            <div class="delete-button">
                            <!-- delete button's icon a tag link and giving the route of controller '/delete-contact' and passing the i.phone
                                by using this link whenever we click on the particular contact it will fetch the phone of that contact
                                like <a href='delete-contact/1111'> -->
                                <a href="/delete-contact/?id=<%= i._id %>">
                                    <i class="fas fa-window-close delete-icon"></i>
                                </a>
                            </div>                                                          
                        </li> 
                <% } %>
            </ul>
    </div>     
    

    <!-- creating a form to get the new contacts -->
    <div class="submit-form">
        <form action="/create-contact" method="POST">
            <input type="text" name="name" placeholder="enter name">
            <input type="number" name="phone" placeholder="enter phone">
    
            <button type="submit">Add Contact</button>
    
        </form>

    </div>
    
    <!-- note: no need to add /assets in the src and href -->
    <script src="/js/home.js"></script>
    

</body>
</html>
